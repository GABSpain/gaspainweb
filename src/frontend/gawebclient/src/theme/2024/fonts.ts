const fontsDef = {
    familiest: {
        default: 'Roboto, sans-serif',
    },
    sizes: {
        default: 14,
    },
    weights: {
        default: 400,
    },
};

export type Fonts = typeof fontsDef;