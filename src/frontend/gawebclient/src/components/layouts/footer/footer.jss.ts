import { createUseStyles } from "react-jss";

export const FooterStyles = createUseStyles({
    container: {
        padding: '20px',
        textAlign: 'center',
        background: "#151515",
        color: "#f0f0f0",
    }
});
        