import { FooterStyles } from "./footer.jss";

export const Footer = () => {
    const styles = FooterStyles();

    return (
        <div className={styles.container}>
            Footer
        </div>
    );
}