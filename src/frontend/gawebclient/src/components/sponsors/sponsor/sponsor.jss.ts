import { createUseStyles } from "react-jss";

export const SponsorStyles = createUseStyles({
    sponsorLogo: {
        objectFit: 'contain',
    },
    image: {
        maxWidth: '250px',
        width: '250px',
    }
})